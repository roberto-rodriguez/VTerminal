<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.voltcash.vterminal">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.EAC" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.auth.AuthTerminalActivity" />
        <activity android:name=".views.home.HomeActivity" />
        <activity
            android:name=".views.tx.CaptureActivity"
            android:label="@string/title_activity_capture"
            android:parentActivityName=".views.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".views.tx.PreviewActivity"
            android:label="@string/title_activity_preview"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".views.tx.CaptureActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".views.tx.ProcessImageActivity"
            android:label="@string/title_activity_process_image"
            android:parentActivityName=".views.tx.PreviewActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name=".views.tx.CaptureBarcodeActivity"
            android:label="@string/title_activity_capture"
            android:parentActivityName=".views.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity android:name=".views.tx.TxActivity"></activity>

        <activity android:name=".util.ReceiptActivity"></activity>

        <activity android:name=".views.auth.LoginActivity"></activity>

        <activity android:name=".views.settings.ClerkSettingsActivity"
            android:parentActivityName=".views.home.HomeActivity">
        </activity>

        <provider
            android:name="com.voltcash.vterminal.util.GenericFileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>
    </application>

</manifest>